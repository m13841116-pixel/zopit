import fetch from 'node-fetch';
(async () => {
  const start = Date.now();
  console.log("Starting...");
  try {
      // we need a valid token to test... wait, I can just mock the endpoint or look at the logic.
  } catch(e) {}
})();
